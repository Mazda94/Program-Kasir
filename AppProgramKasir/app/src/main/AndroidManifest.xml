<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hirano_ali.programkasir">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="UnusedAttribute">
        <activity
            android:name=".TambahPetugasKasir"
            android:label="Tambah Petugas Kasir"></activity>
        <activity
            android:name=".ListBarangActivity"
            android:label="Daftar Barang"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ListCashierActivity"
            android:label="Daftar Petugas Kasir" />
        <activity android:name=".AdminActivity" />
        <activity
            android:name=".TambahBarangActivity"
            android:label="Tambah Barang" />
        <activity
            android:name=".RegisterActivity"
            android:label="Registrasi Akun"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>